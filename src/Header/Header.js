import React from "react";
import "./Header.css";

const Header = () => {
    return (
        <header className="Header">
            <div className="Header-title">Conway's Game of Life!</div>
        </header>
    );
};

export default Header;
